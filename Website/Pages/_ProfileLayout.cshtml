@{
    Layout = "_Layout";
}

<section class="profile-layout">
    <div class="profile-wrapper">
        <h1 class="profile-page-title">@ViewData["Title"]</h1>
        <hr class="section-divider" />
        @RenderBody()
    </div>
</section>

@await RenderSectionAsync("Scripts", required: false)
